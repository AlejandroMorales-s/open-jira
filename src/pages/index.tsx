import { FC } from "react";
import { Typography } from "@mui/material";

const Home: FC = () => {
  return (
    <Typography variant="h1" color="primary">
      ola
    </Typography>
  );
};

export default Home;
